{%- assign product = all_products[handle] -%}{%- if title == 'yes' -%}<h2><a href="{{- product.url | replace: 'products/', 'pages/'-}}">{{- product.title -}}</a></h2>{%- endif -%}{%- if description == 'yes' -%}{{- product.description -}}{%- endif -%}{%- form 'product', product, class:'product-form' -%}<input type="hidden" name="id" value="{{- product.selected_or_first_available_variant.id -}}" /><button type="submit" class="addcart hvr-sweep-to-top">Add to cart</button>{%- endform -%}
